package ninja.samryecroft.returnhome.tracker.dashboard;

/** One entry in the Supplier's "Care provider" switcher - only the providers that Supplier actually serves. */
public record CareProviderOption(Long id, String name) {
}
