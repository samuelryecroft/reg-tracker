package ninja.samryecroft.returnhome.tracker.dashboard;

/** The three preset reporting periods for the 2.3 Performance zone (custom ranges are deliberately out of MVP). */
public enum DashboardPeriod {
    THIS_QUARTER,
    LAST_QUARTER,
    LAST_12_MONTHS
}
